services:
  martinstrategy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: martinstrategy
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
